{
  "name": "elibrary",
  "version": "1.0.0",
  "description": "This project was bootstrapped with [Create React App]",
  "main": "server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node server.js",
    "server": "nodemon server.js",
    "client": "npm run start --prefix the-app",
    "client:install": "npm install --prefix the-app",
    "client:build": "npm run build --prefix the-app",
    "dev": "cross-env NODE_ENV=development concurrently \"npm run server\" \"npm run client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.3",
    "config": "^3.3.8",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^6.14.2",
    "firebase": "^9.13.0",
    "firebase-admin": "^11.2.0"
  },
  "devDependencies": {
    "concurrently": "^7.5.0",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.20"
  }
}
